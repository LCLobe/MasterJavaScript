function computePower(num, exponent) {
  // your code here
    return Math.pow(num, exponent);

   /*  if (exponent === 0) {return 1;}
    else if (exponent === 1) {return num;}
    else {
      let resultado = num;
      for (let i=2; exponent; i++){
          num *= num;
      }
      return num;
  } */
}
console.log(computePower(2, 3));
